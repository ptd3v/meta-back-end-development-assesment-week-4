#Modules
import numpy #Will only work after numpy has been installed

import sys #Importing sys to specify a path
sys.path.insert(1, r'C:\windows\examplefolder\examplefile.py')#1 = First indexed location, r = Used when passing a location as an argument.
#The sys.path list now has a new path to check for modules
